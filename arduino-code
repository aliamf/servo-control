//arduino code
#include <Servo.h>
Servo s1;
Servo s2;
Servo s3;
Servo s4;
Servo s5;
int sPos=90;
void setup()
{
 s1.attach(2);
 s2.attach(3);
 s3.attach(4);
 s4.attach(5);
 s5.attach(6);
}

void loop()
{
  s1.write(sPos);
  s1.write(sPos);
  s1.write(sPos);
  s1.write(sPos);
  s1.write(sPos);
}
